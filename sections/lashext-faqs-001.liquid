<div class="clear"></div>
<section class="faqs">
    <h2 class="faqs-title">{{ section.settings.lashext_faqs_name_001 }}</h2>
    {% for block in section.blocks %}
    <div class="faqs-single">
        <div class="faqs-q">{{ block.settings.lashext_faqs_question }}</div>
        <div class="faqs-a">{{ block.settings.lashext_faqs_answer }}</div>
    </div>
    {% endfor %}
</section>

{% schema %}
{
    "name": "LashEXT FAQs 001",
    "settings": [
        {
            "type": "text",
            "id": "lashext_faqs_name_001",
            "label": "LashEXT FAQs Title 001"
        }
    ],
    "blocks": [
        {
            "type": "component",
            "name": "LashEXT FAQs 001",
            "settings": [
                {
                    "type": "text",
                    "id": "lashext_faqs_question",
                    "label": "LashEXT Question"
                },
                {
                    "type": "textarea",
                    "id": "lashext_faqs_answer",
                    "label": "LashEXT Answer"
                }
            ]
        }
    ]
}
{% endschema %}