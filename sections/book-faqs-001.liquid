<div class="clear"></div>
<section class="faqs">
    <h2 class="faqs-title">{{ section.settings.book_faqs_name_001 }}</h2>
    {% for block in section.blocks %}
    <div class="faqs-single">
        <div class="faqs-q">{{ block.settings.book_faqs_question }}</div>
        <div class="faqs-a">{{ block.settings.book_faqs_answer }}</div>
    </div>
    {% endfor %}
</section>

{% schema %}
{
    "name": "Book FAQs 001",
    "settings": [
        {
            "type": "text",
            "id": "book_faqs_name_001",
            "label": "Book FAQs Title 001"
        }
    ],
    "blocks": [
        {
            "type": "component",
            "name": "Book FAQs 001",
            "settings": [
                {
                    "type": "text",
                    "id": "book_faqs_question",
                    "label": "Book Question"
                },
                {
                    "type": "textarea",
                    "id": "book_faqs_answer",
                    "label": "Book Answer"
                }
            ]
        }
    ]
}
{% endschema %}